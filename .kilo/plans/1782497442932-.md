# Plan: Complete Thumbnaily Deployment & Verification

## Context
Thumbnaily (Next.js + Supabase + Netlify) implements OpenAI Responses API features for AI-powered thumbnail generation. Several features are implemented locally but deployment is blocked due to missing Netlify environment variables.

## Current State
- **Code**: All changes exist in working directory (uncommitted)
- **Netlify site**: `willowy-sundae-6af5cf` (ID `07458cce-22f0-423e-a12b-cb2ee7b14fc0`) linked to GitHub
- **Blocker**: Missing env vars `NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `OPENAI_API_KEY`
- **Latest commit**: `262e20c` (TypeScript fix, missing env vars causes 500)

## Steps

### 1. Commit & Push Remaining Changes
- Stage all uncommitted changes (new files, updated files)
- Commit with descriptive message covering:
  - Prompt coaching, templates, style presets, variants, caption overlay
  - Background replacement, multi-channel resize
  - Thumbnail critic, conversation tree, smart tagging/search filters
  - Streaming SSE for enhance/coach/critic endpoints
  - Image v2 (`gpt-image-2`) with variants and multi-turn conversation
  - Multi-tool orchestration (research/analyze-and-improve modes)
  - Supabase migration for new tables/columns
- Push to main branch to trigger Netlify rebuild

### 2. Set Netlify Environment Variables
- **Current blocker**: Cannot set env vars via API/CLI from sandbox (requires dashboard auth)
- **Action**: Add `NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `OPENAI_API_KEY` via Netlify dashboard UI
- **Supabase project**: `bzxohkrxcwodllketcpz` (service role key needed from user)

### 3. Verification Checklist
After env vars are set and site deploys:
- [ ] `/api/templates` returns 200 with 8 templates + 10 styles
- [ ] `/app` shows Prompt Coach sidebar, Template/Style picker, 4-Variant toggle
- [ ] `/public/[thumbnailid]` displays Critic widget, Caption editor, Background replace, Multi-Channel resize, Conversation tree
- [ ] `/public` shows search bar + faceted filters (mood/palette/tag)
- [ ] Streaming endpoints return `text/event-stream` with `partial-variant` SSE events
- [ ] Image v2 (`gpt-image-2`) generation succeeds
- [ ] Multi-tool mode (`research`, `analyze-and-improve`) executes without errors

## Open Questions
1. **Env vars**: Can user provide Netlify dashboard access or set env vars themselves?
2. **Supabase service role key**: Do we need to run the migration manually in Supabase SQL editor, or expect it to auto-apply via Netlify build?
3. **Deployment**: Once uncommitted changes are pushed, should we verify the new commit deploys successfully before proceeding?

## Considerations
- All features are free-tier compatible (no billing/credits system)
- Auth ticket for Netlify CLI: `https://app.netlify.com/authorize?response_type=ticket&ticket=8c81a652271fd16f367f5f59d23b26f0` (still valid?)
- Sub-agents previously attempted but did not commit streaming/Image v2/multi-tool changes
