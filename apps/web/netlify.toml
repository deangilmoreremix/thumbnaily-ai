[build]
  command = "pnpm install && pnpm prisma generate --schema=../../packages/db/prisma/schema.prisma && pnpm build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"
  NEXT_TELEMETRY_DISABLED = "1"

[[plugins]]
  package = "@netlify/plugin-nextjs"